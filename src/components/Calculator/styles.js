import styled from 'styled-components';
import { BORDER_SIZE_STYLE } from '@constants/StyleConstants';

const CalculatorWrapper = styled.div`
  padding-right: 15px;
  height: 100%;
  width: 100%;
  border-right: ${BORDER_SIZE_STYLE} #717171;
`;

export default CalculatorWrapper;
